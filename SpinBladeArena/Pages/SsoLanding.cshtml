@page "/sso-landing"
@using System.Text.Json
@model SpinBladeArena.Pages.SsoLandingModel
@{
}
<h1>登陆成功😀</h1>

<script src="~/userManager.js"></script>
<script>
    setToken(@Html.Raw(JsonSerializer.Serialize(Model.TokenDto, new JsonSerializerOptions { PropertyNamingPolicy = JsonNamingPolicy.CamelCase } )))
    location.href = '/';
</script>